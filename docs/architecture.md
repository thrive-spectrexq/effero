# Architecture

The authoritative architecture overview — the six-layer diagram, layer-by-layer
breakdown, repository layout, and design principles — lives in the top-level
[`README.md`](../README.md#architecture).

This file is a placeholder for deeper, implementation-level architecture docs
(e.g. the planning loop's state machine, the memory tiering strategy, the
guardrail policy DSL grammar) as those subsystems land. See `ROADMAP` in the
README for sequencing, and feel free to open a PR here alongside the code
that implements a given layer.
